services:
  # ── Node.js Application ─────────────────────
  app:
    build: .
    container_name: url-monitor-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
