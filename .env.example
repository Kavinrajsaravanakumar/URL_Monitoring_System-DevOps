# Server Configuration
PORT=3000

# MongoDB Connection String
MONGO_URI=mongodb://localhost:27017/urlmonitor

# Monitoring interval in cron format (default: every 1 minute)
CRON_SCHEDULE=* * * * *

# Axios timeout for URL checks (in milliseconds)
CHECK_TIMEOUT=10000
